# Dictionary provenance

`starter.tsv` is an authored demonstration vocabulary (165 entries), maintained
with this application. It supplies common phrase rankings and explicit simplified
and traditional forms. It is not a complete Chinese lexicon.

`luna_pinyin.dict.yaml` is the unmodified Rime Luna Pinyin source downloaded on
2026-09-14 from:
https://raw.githubusercontent.com/rime/rime-luna-pinyin/master/luna_pinyin.dict.yaml

Project: https://github.com/rime/rime-luna-pinyin
Copyright and attribution: Rime Developers <rime-devel@googlegroups.com>.
Upstream's source header acknowledges CC-CEDICT, Android PinyinIME, Chewing,
三拼簡繁詞庫, OpenCC, and 萌典. That header is preserved in the bundled source.
Upstream distributes this project under GNU LGPL version 3; the unmodified
license is `LICENSE.rime`, with its incorporated GPL text in `LICENSE.GPL-3.0`.
These terms apply to the Rime source and the derived `rime.tsv` dictionary.

`rime.tsv` is generated by `scripts/build_dictionary.py`. Changes: removal of
YAML metadata; filtering to plain pinyin records; insertion of simplified forms
using OpenCC; assignment of a default weight when upstream supplies no weight.
The original traditional spellings remain in the second TSV column. Both the
source and conversion script are included to allow regeneration/modification.

Regenerate locally after installing opencc-python-reimplemented:

```
python scripts/build_dictionary.py
```

To explicitly update to the latest upstream source, use `--download`. Review
upstream changes and license before redistributing an updated dictionary.

This decoder does not embed librime and does not reproduce Rime's statistical
ranking or preset vocabulary. Dictionary coverage is broad, but rankings are
basic and there is no personalized learning. OpenCC phrase conversion may have
contextual ambiguities. Remove/replace `rime.tsv` to use the starter vocabulary
or an independently supplied TSV (simplified, traditional, spaced pinyin,
weight; tab-separated UTF-8).
